export const TILE_SIZE = 32;
export const INITIAL_LIVES = 3; // Default lives for a new game or after game over
export const MAX_LIVES_ON_LOAD = 10; // Max lives when MainScene is created (e.g. dev default)
